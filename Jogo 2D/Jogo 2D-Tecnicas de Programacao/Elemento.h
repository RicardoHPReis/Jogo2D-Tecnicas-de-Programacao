#pragma once
/*#include "stdafx.h"

template<class EL>
class Elemento
{
private:
	Elemento<EL> *anterior;
	EL *info;
	Elemento<EL> *proximo;

public:
	Elemento();
	~Elemento();

	void setInfo(const EL *informacao);
	const EL* getInfo();

	void setAnterior(const Elemento<EL>* ant);
	const Elemento<EL>* getAnterior();

	void setProximo(const Elemento<EL>* prox);
	const Elemento<EL>* getProximo();
};*/
